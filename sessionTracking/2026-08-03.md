# Session Tracking Log - 2026-08-03

## Session Metadata
- Date: 2026-08-03
- Actor: James

## Entries

### 2026-08-03 22:56
- User request summary: Add pin/persist feature to dev console module
- Files changed:
  - src/debug/devConsole.ts
  - src/managers/GameEngine.ts
  - src/io/debugCommands.ts
  - docs/dev-console.md
- What changed:
  - Added pin/persist support to `openDevConsole` (new `persistKey` option, `show`/`hide`/`isPinned`/`setPinned`/`togglePin` on the handle, localStorage-backed pin + filter state, Pin button in controls, × now hides instead of destroying when pinned)
  - Added `mountPersistentDevConsole(log, opts?)` and wired it into `GameEngine.initDebug()` so a previously pinned console auto-reopens with the same filters
  - Exposed `__gameDebug.console` in `attachDebugApi` (mirrors the active handle for `isOpen`, `isPinned`, `show`, `hide`, `togglePin`, `setPinned`); engine now threads `consoleHandle` + `setConsoleHandle` into the debug API
- Verification run (tests/commands) and result:
  - npm run build (pass)
- Revert notes:
  - git restore src/debug/devConsole.ts src/managers/GameEngine.ts src/io/debugCommands.ts docs/dev-console.md
- Actor: Jake The State Farm

## Entries

### 2026-08-03 02:33
- User request summary: Pull latest changes from GitHub main branch.
- Files changed:
  - (none — already up to date)
- What changed:
  - Switched from Millerfly91/updates-to-combat-documentation-and-fatigue-planning to main branch.
  - Pulled from github/main — already up to date.
  - Removed stale untracked sessionTracking files before switch.
- Verification run (tests/commands) and result:
  - git switch main (pass)
  - git pull github main (pass — Already up to date.)
- Revert notes:
  - No file changes to revert. To return to the feature branch: git switch Millerfly91/updates-to-combat-documentation-and-fatigue-planning

### 2026-08-03 22:27
- User request summary: Fixed GitHub Actions CI smoke test port mismatch between scripts/ports.ps1 dynamic ports and hardcoded ports in tests
- Files changed:
  - test/smoke.ts
  - test/dragDrop.test.ts
- What changed:
  - Replaced hardcoded WEB_PORT=4173 and API_PORT=3001 with `Number(process.env.CLIENT_PORT) || 4173` and `Number(process.env.API_PORT) || 3001` in both test files
  - Fixed dragDrop.test.ts `startApi()` passing `PORT` instead of `API_PORT` to the spawned API server process
- Verification run (tests/commands) and result:
  - (not yet run — changes uncommitted; `npm run test:smoke` + `npm run test:dragdrop` should pass with `npm run dev` running)
- Revert notes:
  - `git restore test/smoke.ts test/dragDrop.test.ts`

### 2026-08-03 23:41
- User request summary: Increment minor version, rebuild, and rerun dev server.
- Files changed:
  - package.json
- What changed:
  - Bumped package.json version from 0.1.0 to 0.2.0.
  - Ran `npm run build` (tsc + vite build) — passed.
  - Restarted dev server via `npm run dev` — no errors.
- Verification run (tests/commands) and result:
  - `npm run build` (pass)
  - `npm run dev` (pass — server started cleanly)
- Revert notes:
  - `git restore package.json`

### 2026-08-03 23:49
- User request summary: Run 3 npm tests simultaneously at 11:45 PM with distinct ports.
- Files changed:
  - `.env` (temporarily modified, restored to defaults)
  - `scripts/scheduled-tests.ps1` (created then cleaned up)
- What changed:
  - Scheduled 3 test runs (smoke on API:4010/CLIENT:5180, cityView on API:4020/CLIENT:5190, smoke on API:4030/CLIENT:5200) via `cmd /c` process launch.
  - All 3 web servers started on their assigned client ports (5180, 5190, 5200).
  - Docker daemon was not running — game_db PostgreSQL container down (ECONNREFUSED on 5432). cityView test API failed to connect to DB. Smoke tests likely partially failed.
  - Cleaned up temporary `scripts/scheduled-tests.ps1` and restored `.env` to defaults.
- Verification run (tests/commands) and result:
  - 3 servers confirmed running on ports 5180, 5190, 5200 (pass)
  - DB connectivity (ECONNREFUSED) — Docker daemon offline (fail)
- Revert notes:
  - `git restore .env` (already restored to defaults). `scripts/scheduled-tests.ps1` already deleted.

